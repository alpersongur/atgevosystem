import 'package:flutter_test/flutter_test.dart';

void main() {
  group('InventoryService', () {
    test(
      'adjustStock prevents negative quantity',
      () {
        // TODO(alper): Fake Firestore ile envanter stok güncellemesini test et.
        expect(true, isTrue);
      },
      skip: 'Mock ortamı hazırlandığında tamamlanacak.',
    );
  });
}
